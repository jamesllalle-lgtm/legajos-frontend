import { NumerotelefonoDirective } from './numerotelefono.directive';

describe('NumerotelefonoDirective', () => {
  it('should create an instance', () => {
    const directive = new NumerotelefonoDirective();
    expect(directive).toBeTruthy();
  });
});
