import { LetrasDirective } from './letras.directive';

describe('LetrasDirective', () => {
  it('should create an instance', () => {
    const directive = new LetrasDirective();
    expect(directive).toBeTruthy();
  });
});
