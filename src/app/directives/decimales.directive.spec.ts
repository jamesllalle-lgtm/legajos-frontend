import { DecimalesDirective } from './decimales.directive';

describe('DecimalesDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalesDirective();
    expect(directive).toBeTruthy();
  });
});
