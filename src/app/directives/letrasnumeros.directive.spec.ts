import { LetrasnumerosDirective } from './letrasnumeros.directive';

describe('LetrasnumerosDirective', () => {
  it('should create an instance', () => {
    const directive = new LetrasnumerosDirective();
    expect(directive).toBeTruthy();
  });
});
