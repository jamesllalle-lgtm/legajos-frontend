import { CallbackidofPipe } from './callbackidof.pipe';

describe('CallbackidofPipe', () => {
  it('create an instance', () => {
    const pipe = new CallbackidofPipe();
    expect(pipe).toBeTruthy();
  });
});
